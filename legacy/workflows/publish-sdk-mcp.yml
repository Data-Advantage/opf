name: Publish pptx-mcp

on:
  push:
    tags:
      - "sdk-mcp-v*"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry-run (skip actual publish)"
        type: boolean
        default: false

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish to npm with provenance
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"

      # pptx-mcp bundles @pptx/sdk via `file:../typescript` and tsup
      # `noExternal`, so the SDK's dist/ must exist before mcp builds.
      - name: Build @pptx/sdk (dependency)
        working-directory: typescript
        run: |
          pnpm install --frozen-lockfile
          pnpm run build

      - name: Install pptx-mcp dependencies
        working-directory: mcp
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        working-directory: mcp
        run: pnpm run typecheck

      - name: Build
        working-directory: mcp
        run: pnpm run build

      - name: Verify tag matches package version
        if: startsWith(github.ref, 'refs/tags/sdk-mcp-v')
        working-directory: mcp
        run: |
          TAG="${GITHUB_REF_NAME#sdk-mcp-v}"
          PKG=$(node -p "require('./package.json').version")
          if [ "$TAG" != "$PKG" ]; then
            echo "::error::Tag version ($TAG) does not match package.json version ($PKG)"
            exit 1
          fi

      - name: Publish (dry-run)
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true'
        working-directory: mcp
        run: npm publish --provenance --access public --dry-run

      - name: Publish
        if: github.event_name != 'workflow_dispatch' || github.event.inputs.dry_run != 'true'
        working-directory: mcp
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance --access public
