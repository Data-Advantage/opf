"""Optional framework adapters (LangChain, LlamaIndex).

These modules are intentionally isolated in a subpackage. Importing the
framework adapter requires the corresponding optional dependency::

    pip install "pptx-dev[langchain]"
    pip install "pptx-dev[llamaindex]"
"""
