{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pptx.dev/schema/opf/v1",
  "title": "OPF — Open Presentation Format",
  "description": "Open-source JSON format for describing PowerPoint presentations. Agents describe narratives, content, audience, and design — the engine handles OOXML complexity.\n\nFields that reference catalog records (design.theme, design.colors.scheme, design.fonts.scheme, Slide.layout, chart.chartPresetId, meta.language, meta.narrative.template) resolve through the catalog system: inline 'catalogs.<kind>.records[]' on the document → 'catalogs.<kind>.source' on the document → engine defaults → the default catalog at https://www.pptx.gallery/<kind>. When an OPF document omits a reference entirely, the engine falls back to its 'defaults.json' (see /spec/defaults.json for a reference example). The defaults file is engine configuration; it is not part of the OPF document contract and has no JSON Schema.",
  "type": "object",
  "required": ["$schema", "meta", "design", "slides"],
  "properties": {
    "$schema": {
      "type": "string",
      "const": "https://pptx.dev/schema/opf/v1",
      "description": "Identifies the OPF schema version so validators and editors can resolve the correct rules."
    },
    "meta": {
      "$ref": "#/$defs/Meta",
      "description": "High-level presentation metadata such as title, speakers, organizations, audience, and narrative."
    },
    "design": {
      "$ref": "#/$defs/Design",
      "description": "Design system covering colors, fonts, dimensions, background, brand, and layout preferences applied to the deck."
    },
    "slides": {
      "type": "array",
      "items": { "$ref": "#/$defs/Slide" },
      "minItems": 1,
      "description": "Ordered array of slides that make up the presentation."
    },
    "catalogs": {
      "$ref": "#/$defs/Catalogs",
      "description": "Optional per-kind catalog overrides. Each kind may declare a non-default 'source' and/or inline 'records' that override or supplement the default catalog at https://www.pptx.gallery/<kind>. References elsewhere in the document (e.g. design.theme, meta.narrative.template) resolve through this section before falling back to the default catalog."
    },
    "extensions": {
      "type": "object",
      "description": "Custom data passthrough for agent workflows; ignored by the engine but preserved across read/write round-trips."
    }
  },
  "$defs": {
    "Meta": {
      "type": "object",
      "description": "Presentation-level metadata describing what the deck is, who it's for, and how it should be told.",
      "required": ["title"],
      "properties": {
        "title": {
          "type": "string",
          "description": "Display title of the presentation.",
          "examples": ["Q4 2026 Business Review", "Series B Pitch — Acme", "The Future of Agentic Workflows"]
        },
        "description": {
          "type": "string",
          "description": "Free-form prose describing what this presentation is about. Used by agents and humans as a deck-level summary; complements meta.purpose (the goal) and meta.narrative (the structured storyline).",
          "examples": ["Quarterly business review covering revenue, product, and hiring milestones for the executive team."]
        },
        "filename": {
          "type": "string",
          "description": "Optional base filename for exports (without extension). Engine strips a trailing .pptx, .pdf, .png, or .svg (case-insensitive) and appends the target format's extension. When omitted, the engine slugifies meta.title. See /docs/opf for full validation rules.",
          "examples": ["q4-2026-business-review", "acme-series-b-pitch"]
        },
        "subtitle": {
          "type": "string",
          "description": "Optional subtitle shown alongside the title, typically on cover slides.",
          "examples": ["Prepared for the Board of Directors", "October 2026"]
        },
        "organizations": {
          "type": "array",
          "items": { "$ref": "#/$defs/Organization" },
          "description": "Organizations associated with the presentation (e.g., the presenting company, host conference, partners, clients, sponsors). The 'primary' organization (declared via Organization.role or, if no role is set, the first item) drives default branding such as cover-slide logo. Optional — simple decks may omit this."
        },
        "speakers": {
          "type": "array",
          "items": { "$ref": "#/$defs/Speaker" },
          "description": "People presenting the deck. Used for cover slides, bio slides, footers, and panel attribution. Optional — simple decks may omit this."
        },
        "authors": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional credit list for people who authored or contributed to the deck, distinct from speakers. Use when the writer and presenter differ (e.g., analyst-written, exec-presented).",
          "examples": [["Bob Lee", "Carla Diaz"]]
        },
        "audience": {
          "type": "string",
          "description": "Intended audience for the presentation. Used by AI to tune tone and depth.",
          "examples": ["executives", "engineering team", "Series B investors", "board of directors"]
        },
        "purpose": {
          "type": "string",
          "description": "Primary goal of the presentation. Complements meta.description and meta.narrative.",
          "examples": ["Raise a Series B round of $30M", "Align the engineering org on Q4 priorities", "Educate customers on the new pricing model"]
        },
        "narrative": {
          "$ref": "#/$defs/Narrative",
          "description": "Structured storyline describing the deck's arc, key messages, and tone."
        },
        "language": {
          "type": "string",
          "description": "Language for the presentation content. Accepts either:\n- A BCP-47 language tag ('en-US', 'ja-JP', 'fr').\n- A catalog reference resolved against catalogs.languages and the default catalog at https://www.pptx.gallery/languages — a bare id (lowercase kebab-case, e.g. 'english'), an HTTPS URL pointing at a record file, or a 'pkg:' reference to a locally-installed package.\n\nResolution order for the catalog form: inline catalogs.languages.records[] → catalogs.languages.source → default catalog.",
          "examples": ["en-US", "en-GB", "es-MX", "ja-JP", "fr", "english", "japanese", "https://acme.com/decks/languages/en-acme.json", "pkg:@acme/decks/languages/en-acme"]
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Free-form labels used for categorization, search, and filtering. Lowercase kebab-case is recommended for consistency across a deck library.",
          "examples": [["qbr", "internal", "fy26-q4"], ["pitch", "fundraising", "confidential"]]
        }
      }
    },
    "Organization": {
      "type": "object",
      "description": "An organization associated with the presentation — typically the presenting company, but also hosts, partners, clients, or sponsors. Surfaced on cover slides, footers, and brand bars; the primary organization's logo is the default for design.brand.logo unless overridden.",
      "required": ["id", "name"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-zA-Z0-9_-]+$",
          "description": "Stable identifier for the organization, used to reference it from Speaker.organizationId. Must be unique within the deck.",
          "examples": ["acme", "acme-corp", "devconf-2026"]
        },
        "name": {
          "type": "string",
          "description": "Display name shown on slides.",
          "examples": ["Acme Corp", "DevConf 2026", "Beta Industries"]
        },
        "legalName": {
          "type": "string",
          "description": "Optional legal entity name when it differs from the display name.",
          "examples": ["Acme Corporation, Inc.", "Beta Industries, LLC"]
        },
        "logo": {
          "type": "string",
          "description": "Source for the organization's logo image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference. Common formats are SVG (preferred for vector logos), PNG (with transparency), or JPG. The primary organization's logo is used by default for cover-slide and footer branding; design.brand.logo overrides the source.",
          "examples": [
            "https://cdn.acme.com/logo.svg",
            "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAusB9V3iWggAAAAASUVORK5CYII=",
            "./assets/logo.jpg",
            "asset_acme_logo"
          ]
        },
        "domain": {
          "type": "string",
          "description": "Bare internet domain for the organization. Used for footers, contact slides, and engine-driven asset lookups (e.g., favicon-based brand defaults).",
          "examples": ["acme.com", "beta-industries.io", "devconf.org"]
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "General contact email for the organization. Used on contact slides and footer attribution.",
          "examples": ["hello@acme.com", "sales@acme.com", "info@beta-industries.io"]
        },
        "phone": {
          "type": "string",
          "description": "Main contact phone number for the organization. E.164 format is recommended.",
          "examples": ["+14155551234", "+442071838750"]
        },
        "tagline": {
          "type": "string",
          "description": "Short tagline rendered alongside the organization name on cover slides.",
          "examples": ["Build the future of work", "Infrastructure for AI agents", "Where teams ship faster"]
        },
        "role": {
          "type": "string",
          "enum": ["primary", "partner", "client", "sponsor", "host"],
          "description": "Role of the organization relative to the presentation. When omitted, the first organization in the array is treated as primary."
        },
        "socials": {
          "$ref": "#/$defs/Socials",
          "description": "Optional social media handles or URLs for the organization."
        }
      }
    },
    "Speaker": {
      "type": "object",
      "description": "A person presenting the deck. Used for cover slides, bio/intro slides, footer attribution, and panel formats with multiple presenters.",
      "required": ["id", "name"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-zA-Z0-9_-]+$",
          "description": "Stable identifier for the speaker, used for cross-references within the deck. Must be unique within the deck.",
          "examples": ["alice", "alice-chen", "speaker-1"]
        },
        "name": {
          "type": "string",
          "description": "Display name.",
          "examples": ["Alice Chen", "Bob Lee", "Dr. Carla Diaz"]
        },
        "title": {
          "type": "string",
          "description": "Role or title. Often paired with the speaker's organization on cover slides.",
          "examples": ["VP of Engineering", "Founder & CEO", "Chief Product Officer", "Senior Staff Engineer"]
        },
        "photo": {
          "type": "string",
          "description": "Source for the speaker's headshot image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference. Common formats are JPG or PNG; SVG is not appropriate for photographic content. Used on cover and bio slides.",
          "examples": [
            "https://cdn.acme.com/headshots/alice.jpg",
            "data:image/jpeg;base64,",
            "./assets/photos/alice.png",
            "asset_photo_alice"
          ]
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "Contact email, used on contact slides or footer attribution when appropriate.",
          "examples": ["alice@acme.com", "alice.chen@acme.com"]
        },
        "phone": {
          "type": "string",
          "description": "Contact phone number for the speaker. E.164 format is recommended.",
          "examples": ["+14155551234", "+442071838750"]
        },
        "bio": {
          "type": "string",
          "description": "Short biographical paragraph for bio or 'about the speaker' slides.",
          "examples": [
            "Alice leads engineering at Acme, where she's spent the last six years scaling distributed systems from prototype to billions of requests per day. Previously, she was a staff engineer at Google."
          ]
        },
        "organizationId": {
          "type": "string",
          "description": "Reference to an Organization.id in meta.organizations. Lets a speaker be attributed to their org in panel or multi-org decks without repeating organization details.",
          "examples": ["acme", "beta", "devconf-2026"]
        },
        "socials": {
          "$ref": "#/$defs/Socials",
          "description": "Optional social media handles or URLs for the speaker."
        }
      }
    },
    "Socials": {
      "type": "object",
      "description": "Social media handles or URLs. Every property is optional; consumers may render values either as full URLs or as platform handles (e.g., '@acme'). Use this both for organizations and individual speakers.",
      "properties": {
        "linkedin": {
          "type": "string",
          "description": "LinkedIn profile or company page URL or handle.",
          "examples": ["https://linkedin.com/in/alice-chen", "https://linkedin.com/company/acme"]
        },
        "x": {
          "type": "string",
          "description": "X (formerly Twitter) profile URL or handle.",
          "examples": ["@alicechen", "https://x.com/acme"]
        },
        "github": {
          "type": "string",
          "description": "GitHub user or organization URL or handle.",
          "examples": ["alicechen", "https://github.com/acme"]
        },
        "youtube": {
          "type": "string",
          "description": "YouTube channel URL or handle.",
          "examples": ["@acme", "https://youtube.com/@acme"]
        },
        "instagram": {
          "type": "string",
          "description": "Instagram profile URL or handle.",
          "examples": ["@acme", "https://instagram.com/acme"]
        },
        "facebook": {
          "type": "string",
          "description": "Facebook page URL or handle.",
          "examples": ["acme", "https://facebook.com/acme"]
        },
        "tiktok": {
          "type": "string",
          "description": "TikTok profile URL or handle.",
          "examples": ["@acme", "https://tiktok.com/@acme"]
        },
        "threads": {
          "type": "string",
          "description": "Threads profile URL or handle.",
          "examples": ["@alicechen", "https://threads.net/@acme"]
        },
        "mastodon": {
          "type": "string",
          "description": "Mastodon profile URL (include the instance, e.g., 'https://mastodon.social/@user').",
          "examples": ["https://mastodon.social/@alice", "https://hachyderm.io/@acme"]
        },
        "bluesky": {
          "type": "string",
          "description": "Bluesky profile URL or handle (e.g., 'user.bsky.social').",
          "examples": ["alice.bsky.social", "https://bsky.app/profile/acme.bsky.social"]
        }
      }
    },
    "Narrative": {
      "type": "object",
      "description": "Structured storyline metadata used by AI to shape generated content. Narrative declares the deck's intended story arc; slides may opt into beats via Slide.beat. The narrative does not constrain slide structure — validators warn on drift (orphan slides, unused beats) but never error. Slides are the source of truth; narrative is intent that travels with the deck.",
      "properties": {
        "template": {
          "type": "string",
          "description": "Catalog reference resolved against catalogs.narratives and the default catalog at https://www.pptx.gallery/narratives. Accepts a bare id (lowercase kebab-case, e.g. 'classic-story'), an HTTPS URL pointing at a record file, or a 'pkg:' reference to a locally-installed package.\n\nResolution order: inline catalogs.narratives.records[] → catalogs.narratives.source → default catalog. Unknown ids produce a validation warning, not an error.",
          "examples": ["problem-solution", "scqa", "pitch-deck", "qbr", "weekly-progress", "classic-story", "https://acme.com/decks/narratives/founder-pitch.json", "pkg:@acme/decks/narratives/founder-pitch"]
        },
        "description": {
          "type": "string",
          "description": "Free-form narrative description used for AI-driven generation.",
          "examples": [
            "Open with the cost of slow agentic workflows, contrast with what becomes possible at sub-second latency, then walk through our architecture and benchmark results, ending with a concrete adoption ask."
          ]
        },
        "keyMessages": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Key messages the presentation should convey.",
          "examples": [[
            "Agent latency is the new bottleneck for AI products.",
            "Our runtime is 8x faster than the next-best option.",
            "Customers see ROI within the first quarter."
          ]]
        },
        "tone": {
          "type": "string",
          "enum": ["formal", "casual", "inspirational", "technical", "persuasive"],
          "description": "Desired tone for the presentation."
        },
        "durationMinutes": {
          "type": "number",
          "minimum": 1,
          "description": "Target presentation duration in minutes."
        },
        "beats": {
          "type": "array",
          "items": { "$ref": "#/$defs/NarrativeBeat" },
          "description": "Inline narrative beats. When provided alongside a 'template', beats here override or extend matching template beats by 'id'. Without 'template', this defines a fully custom narrative arc. Beat IDs must be unique within the document."
        }
      }
    },
    "NarrativeBeat": {
      "type": "object",
      "description": "A single narrative beat — a labeled segment of the story arc with a specific dramatic purpose (e.g. 'hook', 'problem', 'evidence', 'ask'). Slides reference beats via Slide.beat. Beats may also carry slide-blueprint hints (slideType, layoutHint, placeholders, options, thoughtCues, instructions) that pre-shape the generated slide.",
      "required": ["id", "name"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Stable slug used by Slide.beat to reference this beat. Lowercase kebab-case.",
          "examples": ["hook", "problem", "evidence", "ask", "next-steps"]
        },
        "name": {
          "type": "string",
          "description": "Human-readable beat name.",
          "examples": ["The Hook", "The Problem", "Why Now", "The Ask"]
        },
        "description": {
          "type": "string",
          "description": "Curator-written prose that explains what this beat should accomplish. Used as a prompt for AI-driven generation of slides assigned to this beat.",
          "examples": [
            "Quantify the pain customers feel today, with one striking stat the audience can repeat afterward."
          ]
        },
        "instructions": {
          "type": "string",
          "description": "Short author-facing instruction for the beat — typically one phrase. Complements 'description'; AI uses this as a directive while 'description' supplies the rationale.",
          "examples": ["Capture audience attention", "Introduce problem", "Detail implementation", "Inspire & conclude"]
        },
        "slideCount": {
          "type": "integer",
          "minimum": 1,
          "description": "Optional explicit slide count for this beat. Defaults to 1 when omitted; values >1 are reserved for beats that intentionally span multiple slides. Prefer decomposing a heavy beat into multiple beats over setting a high slideCount. The validator emits a warning if the deck's actual count differs significantly."
        },
        "slideType": {
          "type": "string",
          "enum": ["text", "image", "chart", "table", "code"],
          "description": "Default content kind for the beat's slide. Mirrors Element.type and helps engines choose a sensible layout when only the beat is specified."
        },
        "layoutHint": {
          "type": "string",
          "description": "Suggested layout id for the beat's opening slide. Resolves the same way as Slide.layout — against catalogs.layouts and the default catalog at https://www.pptx.gallery/layouts.",
          "examples": ["section-divider", "title-slide", "title-left", "two-column", "text-left"]
        },
        "placeholders": {
          "$ref": "#/$defs/NarrativeBeatPlaceholders",
          "description": "Which slide placeholders the beat expects to be filled. Helps the engine and AI decide what content to generate."
        },
        "options": {
          "$ref": "#/$defs/NarrativeBeatOptions",
          "description": "Beat rendering options that shape how the resolved layout is populated."
        },
        "thoughtCues": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional speaker prompts or thinking cues attached to the beat. Surfaced in presenter notes or AI prompts.",
          "examples": [["What pain is the audience feeling right now?", "Why hasn't anyone solved this yet?"]]
        }
      }
    },
    "NarrativeBeatPlaceholders": {
      "type": "object",
      "description": "Boolean flags indicating which standard slide placeholders the beat expects to fill. Engines may use these as content-generation hints; missing flags are treated as 'unspecified', not 'false'.",
      "properties": {
        "tag":        { "type": "boolean", "description": "Whether to render a small tag / label above the title.", "examples": [false, true] },
        "title":      { "type": "boolean", "description": "Whether the beat fills the slide title.", "examples": [true, false] },
        "subtitle":   { "type": "boolean", "description": "Whether the beat fills the slide subtitle.", "examples": [false, true] },
        "slideImage": { "type": "boolean", "description": "Whether the beat carries a dedicated slide-level image (separate from any content image).", "examples": [false, true] }
      }
    },
    "NarrativeBeatOptions": {
      "type": "object",
      "description": "Engine-facing options that fine-tune how a beat's resolved layout is populated.",
      "properties": {
        "multiple": {
          "type": "string",
          "enum": ["1x", "2x", "3x", "4x", "5x", "6x"],
          "description": "How many parallel content blocks the beat expects in its layout (e.g. '2x' for a two-column beat)."
        },
        "showLabel": {
          "type": "boolean",
          "description": "Whether to render the beat's label/tag on the slide.",
          "examples": [true, false]
        },
        "showDescription": {
          "type": "boolean",
          "description": "Whether to render the beat's description as supporting copy on the slide.",
          "examples": [true, false]
        }
      }
    },
    "Design": {
      "type": "object",
      "description": "Visual design system applied to the presentation; individual slides may override fields via Slide.designOverrides.",
      "properties": {
        "theme": {
          "type": "string",
          "description": "Catalog reference resolved against catalogs.themes and the default catalog at https://www.pptx.gallery/themes. Accepts a bare id (lowercase kebab-case, e.g. 'minimal'), an HTTPS URL pointing at a record file, or a 'pkg:' reference to a locally-installed package. Inline overrides on design.colors / design.fonts / design.background / design.dimensions take precedence over the resolved theme.\n\nResolution order: inline catalogs.themes.records[] → catalogs.themes.source → default catalog.",
          "examples": ["minimal", "classic", "dark", "bold", "corporate-minimal", "ocean-depth", "https://acme.com/decks/themes/acme-brand.json", "pkg:@acme/decks/themes/acme-brand"]
        },
        "colors": {
          "$ref": "#/$defs/ColorScheme",
          "description": "Color palette for the presentation."
        },
        "fonts": {
          "$ref": "#/$defs/FontScheme",
          "description": "Typography selections for heading, body, accent, and code text."
        },
        "dimensions": {
          "$ref": "#/$defs/Dimensions",
          "description": "Slide dimensions and aspect ratio."
        },
        "background": {
          "$ref": "#/$defs/Background",
          "description": "Default slide background applied across the deck unless overridden on a slide."
        },
        "brand": {
          "$ref": "#/$defs/Brand",
          "description": "Visual brand assets applied to slides (watermark, and an optional logo override). Organization identity (name, primary logo) lives in meta.organizations; design.brand only overrides visual rendering."
        },
        "layoutPreferences": {
          "$ref": "#/$defs/LayoutPreferences",
          "description": "Hints that influence layout decisions across slides."
        }
      }
    },
    "ColorScheme": {
      "type": "object",
      "description": "Color palette used by the design system. Two parallel models are supported and may be mixed:\n\n- OOXML slots (accent1–accent6, dark1, dark2, light1, light2, hyperlink, followedHyperlink) — these mirror the 12-slot PowerPoint theme model and round-trip directly to OOXML. Use these when you want full control over the palette as PowerPoint sees it.\n- Abstract roles (primary, secondary, accent, background, surface, text, textSecondary, custom) — semantic aliases the engine maps onto the OOXML slots. Convenient for inline overrides without thinking in slot numbers.\n\nReference a named scheme via 'scheme' and/or override individual fields below; field overrides take precedence over the resolved scheme.",
      "properties": {
        "scheme": {
          "type": "string",
          "description": "Catalog reference resolved against catalogs.colorSchemes and the default catalog at https://www.pptx.gallery/color-schemes. Accepts a bare id (lowercase kebab-case, e.g. 'cool-horizon'), an HTTPS URL pointing at a record file, or a 'pkg:' reference to a locally-installed package. Slot and role overrides on the surrounding ColorScheme object take precedence over the resolved scheme.\n\nResolution order: inline catalogs.colorSchemes.records[] → catalogs.colorSchemes.source → default catalog.",
          "examples": ["cool-horizon", "boost", "burnt-orange", "ocean-depth", "corporate-blue", "https://acme.com/decks/color-schemes/acme.json", "pkg:@acme/decks/color-schemes/acme"]
        },
        "accent1": {
          "type": "string",
          "description": "Accent 1 color (hex). Mirrors the OOXML accent1 slot.",
          "examples": ["#2874A6", "#FD3223", "#F77F00"]
        },
        "accent2": {
          "type": "string",
          "description": "Accent 2 color (hex). Mirrors the OOXML accent2 slot.",
          "examples": ["#1B4F72", "#0308DB", "#D62828"]
        },
        "accent3": {
          "type": "string",
          "description": "Accent 3 color (hex). Mirrors the OOXML accent3 slot.",
          "examples": ["#5499C7", "#4F4955", "#003049"]
        },
        "accent4": {
          "type": "string",
          "description": "Accent 4 color (hex). Mirrors the OOXML accent4 slot.",
          "examples": ["#7BDBB2", "#A1DB30", "#FCBF49"]
        },
        "accent5": {
          "type": "string",
          "description": "Accent 5 color (hex). Mirrors the OOXML accent5 slot.",
          "examples": ["#3AC67A", "#0682FE", "#EAE2B7"]
        },
        "accent6": {
          "type": "string",
          "description": "Accent 6 color (hex). Mirrors the OOXML accent6 slot.",
          "examples": ["#24A89E", "#FC03BE", "#BFBFBF"]
        },
        "dark1": {
          "type": "string",
          "description": "Dark 1 color (hex). Typically the deepest neutral; OOXML dark1.",
          "examples": ["#000000"]
        },
        "dark2": {
          "type": "string",
          "description": "Dark 2 color (hex). Secondary dark; OOXML dark2.",
          "examples": ["#011842", "#2C2C2C"]
        },
        "light1": {
          "type": "string",
          "description": "Light 1 color (hex). Typically the slide canvas; OOXML lt1.",
          "examples": ["#FFFFFF"]
        },
        "light2": {
          "type": "string",
          "description": "Light 2 color (hex). Secondary light surface; OOXML lt2.",
          "examples": ["#F0F0F0", "#F3F7FF"]
        },
        "hyperlink": {
          "type": "string",
          "description": "Hyperlink color (hex). OOXML hlink.",
          "examples": ["#0563C1", "#0066CC"]
        },
        "followedHyperlink": {
          "type": "string",
          "description": "Followed-hyperlink color (hex). OOXML folHlink.",
          "examples": ["#954F72", "#551A8B"]
        },
        "primary": {
          "type": "string",
          "description": "Abstract role: primary brand color (hex). The engine maps this onto an OOXML accent slot when serializing.",
          "examples": ["#1E40AF", "#0F172A"]
        },
        "secondary": {
          "type": "string",
          "description": "Abstract role: secondary brand color (hex).",
          "examples": ["#3B82F6", "#64748B"]
        },
        "accent": {
          "type": "string",
          "description": "Abstract role: accent color used for highlights and emphasis (hex).",
          "examples": ["#F59E0B", "#22D3EE"]
        },
        "background": {
          "type": "string",
          "description": "Abstract role: default slide background color (hex). The engine maps this to one of light1 / light2 / dark1 / dark2 when serializing.",
          "examples": ["#FFFFFF", "#0B1220"]
        },
        "surface": {
          "type": "string",
          "description": "Abstract role: color for elevated surfaces such as cards and panels (hex).",
          "examples": ["#F8FAFC", "#1E293B"]
        },
        "text": {
          "type": "string",
          "description": "Abstract role: primary body text color (hex).",
          "examples": ["#0F172A", "#F8FAFC"]
        },
        "textSecondary": {
          "type": "string",
          "description": "Abstract role: secondary or muted text color used for captions and supporting copy (hex).",
          "examples": ["#475569", "#94A3B8"]
        },
        "custom": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Map of custom named colors for advanced or theme-specific use.",
          "examples": [{ "success": "#10B981", "warning": "#F59E0B", "danger": "#EF4444" }]
        }
      }
    },
    "FontScheme": {
      "type": "object",
      "description": "Typography selections used by the design system. Two parallel models are supported and may be mixed:\n\n- OOXML pairs (major, minor) — heading and body family names that mirror the PowerPoint majorFont/minorFont entries and round-trip directly to OOXML. The optional 'app', 'languageFamily', and 'type' fields refine which app and script family the pairing targets.\n- Abstract roles (heading, body, accent, code) — semantic Font objects the engine maps onto the OOXML pair when serializing. Convenient for inline overrides and for adding accent/code roles that don't have a direct OOXML slot.\n\nReference a named scheme via 'scheme' and/or override individual fields below; field overrides take precedence over the resolved scheme.",
      "properties": {
        "scheme": {
          "type": "string",
          "description": "Catalog reference resolved against catalogs.fontSchemes and the default catalog at https://www.pptx.gallery/font-schemes. Accepts a bare id (lowercase kebab-case, e.g. 'aptos'), an HTTPS URL pointing at a record file, or a 'pkg:' reference to a locally-installed package. Field overrides on the surrounding FontScheme object take precedence over the resolved scheme.\n\nResolution order: inline catalogs.fontSchemes.records[] → catalogs.fontSchemes.source → default catalog.",
          "examples": ["aptos", "tenorite", "seaford", "noto-sans", "modern-sans", "classic-serif", "https://acme.com/decks/font-schemes/acme.json", "pkg:@acme/decks/font-schemes/acme"]
        },
        "major": {
          "type": "string",
          "description": "Heading (major) font family — mirrors the OOXML majorFont entry. Pairs with 'minor'.",
          "examples": ["Aptos Display", "Calibri", "Microsoft YaHei", "Arial Black"]
        },
        "minor": {
          "type": "string",
          "description": "Body (minor) font family — mirrors the OOXML minorFont entry. Pairs with 'major'.",
          "examples": ["Aptos", "Calibri", "Microsoft YaHei", "Arial"]
        },
        "type": {
          "type": "string",
          "enum": ["sans-serif", "serif", "monospace"],
          "description": "High-level typographic class of the scheme."
        },
        "app": {
          "type": "string",
          "enum": ["PowerPoint", "Google Slides"],
          "description": "Target application this font pairing is intended for."
        },
        "languageFamily": {
          "type": "string",
          "enum": ["latin", "ea", "cs"],
          "description": "OOXML font-language family this scheme is intended for: 'latin' for Latin-script content, 'ea' for East Asian scripts, 'cs' for Complex Scripts."
        },
        "heading": {
          "$ref": "#/$defs/Font",
          "description": "Abstract role: font used for slide titles and headings. Maps onto the OOXML major slot when serializing."
        },
        "body": {
          "$ref": "#/$defs/Font",
          "description": "Abstract role: font used for body copy. Maps onto the OOXML minor slot when serializing."
        },
        "accent": {
          "$ref": "#/$defs/Font",
          "description": "Abstract role: font used for accent text such as quotes or callouts. No direct OOXML slot."
        },
        "code": {
          "$ref": "#/$defs/Font",
          "description": "Abstract role: monospaced font used for code blocks. No direct OOXML slot."
        }
      }
    },
    "Font": {
      "type": "object",
      "description": "Specification for a single font role.",
      "required": ["family"],
      "properties": {
        "family": {
          "type": "string",
          "description": "Font family name.",
          "examples": ["Inter", "Roboto", "Source Serif Pro", "JetBrains Mono"]
        },
        "weight": {
          "type": "number",
          "description": "Numeric font weight (e.g., 400 for regular, 700 for bold)."
        },
        "style": {
          "type": "string",
          "enum": ["normal", "italic"],
          "description": "Font style."
        },
        "letterSpacing": {
          "type": "number",
          "description": "Letter spacing (tracking) in ems."
        }
      }
    },
    "Dimensions": {
      "type": "object",
      "description": "Slide dimensions; either pick a preset or specify custom inches.",
      "properties": {
        "preset": {
          "type": "string",
          "enum": ["16:9", "4:3", "16:10", "letter", "a4", "widescreen", "standard"],
          "description": "Named dimension preset; chooses both aspect ratio and physical size. 'widescreen' is an alias for 16:9 in PowerPoint widescreen size; 'standard' is an alias for 4:3 in PowerPoint standard size."
        },
        "widthInches": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Custom slide width in inches; overrides the preset width when provided."
        },
        "heightInches": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Custom slide height in inches; overrides the preset height when provided."
        }
      }
    },
    "Background": {
      "type": "object",
      "description": "Background fill applied to slides.",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["solid", "gradient", "image", "pattern"],
          "description": "Background fill type."
        },
        "color": {
          "type": "string",
          "description": "Solid fill color (used when type is 'solid'). Accepts either a hex string ('#0F172A') or a theme-color token resolved against the active color scheme: 'light1', 'light2', 'dark1', 'dark2', 'accent1'–'accent6', 'hyperlink', or 'followedHyperlink'. Tokens are resolved at render time and pick up theme overrides automatically.",
          "examples": ["#FFFFFF", "#0F172A", "#F8FAFC", "light1", "dark2", "accent1"]
        },
        "gradient": {
          "type": "object",
          "description": "Gradient fill definition (used when type is 'gradient').",
          "properties": {
            "angle": {
              "type": "number",
              "description": "Gradient angle in degrees (0 = left-to-right, 90 = top-to-bottom)."
            },
            "stops": {
              "type": "array",
              "description": "Ordered list of color stops along the gradient.",
              "items": {
                "type": "object",
                "description": "Color stop along the gradient.",
                "required": ["color", "position"],
                "properties": {
                  "color": {
                    "type": "string",
                    "description": "Stop color as a hex string.",
                    "examples": ["#1E40AF", "#22D3EE"]
                  },
                  "position": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Stop position along the gradient, from 0 (start) to 1 (end)."
                  }
                }
              }
            }
          }
        },
        "image": {
          "type": "object",
          "description": "Image fill definition (used when type is 'image').",
          "required": ["src"],
          "properties": {
            "src": {
              "type": "string",
              "description": "Source for the background image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference.",
              "examples": [
                "https://cdn.acme.com/backgrounds/cover.jpg",
                "data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQABAAD/2wBDAA...",
                "./backgrounds/cover.jpg",
                "asset_bg_cover"
              ]
            },
            "fit": {
              "type": "string",
              "enum": ["cover", "contain", "tile"],
              "description": "How the image fills the slide background."
            }
          }
        },
        "opacity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Background opacity from 0 (fully transparent) to 1 (fully opaque)."
        }
      }
    },
    "Brand": {
      "type": "object",
      "description": "Visual brand assets surfaced across slides. Organization identity (name, primary logo) lives in meta.organizations; this object only carries visual overrides and decorative marks. All logo / icon / wordmark fields are bare source strings — placement and sizing are determined by slide layouts, not by this object.\n\nThe schema offers three asset types (logo, icon, wordmark) plus a decorative watermark, and for each, two themed variants (Light, Dark). The logo additionally supports a 'Stacked' aspect (icon-over-wordmark, suited to vertical / square slots). Naming follows brand-asset-library convention:\n\n- 'Light' = light-colored variant (e.g., white SVG), intended for rendering on dark backgrounds.\n- 'Dark' = dark-colored variant (e.g., black SVG), intended for rendering on light backgrounds.\n- 'Stacked' = vertical lockup with the icon stacked above the wordmark; the unsuffixed forms are assumed to be horizontal lockups.\n- The unsuffixed field (logo / icon / wordmark / watermark) is the default fallback when no themed variant is needed.\n\nEngine resolution: on a dark slide background, prefer the '*Light' variant; on a light slide background, prefer the '*Dark' variant; for vertical / square brand-mark slots, prefer the 'logoStacked*' family over the horizontal logo; otherwise fall back to the unsuffixed asset and (for the logo) ultimately to meta.organizations[primary].logo.",
      "properties": {
        "logo": {
          "type": "string",
          "description": "Default full-lockup logo. Optional override of meta.organizations[primary].logo. Used as the fallback when a themed (Light/Dark) variant isn't set. Accepts an HTTPS URL, data URI, relative path, or asset reference.",
          "examples": [
            "https://cdn.acme.com/logo.svg",
            "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAusB9V3iWggAAAAASUVORK5CYII=",
            "./assets/logo.png",
            "asset_brand_logo"
          ]
        },
        "logoLight": {
          "type": "string",
          "description": "Light-colored full-lockup logo (e.g., white SVG) intended for rendering on dark backgrounds. Accepts an HTTPS URL, data URI, relative path, or asset reference.",
          "examples": [
            "https://cdn.acme.com/logo-white.svg",
            "./assets/logo-white.png",
            "asset_brand_logo_light"
          ]
        },
        "logoDark": {
          "type": "string",
          "description": "Dark-colored full-lockup logo (e.g., black SVG) intended for rendering on light backgrounds. Accepts an HTTPS URL, data URI, relative path, or asset reference.",
          "examples": [
            "https://cdn.acme.com/logo-black.svg",
            "./assets/logo-black.png",
            "asset_brand_logo_dark"
          ]
        },
        "logoStacked": {
          "type": "string",
          "description": "Stacked (vertical) lockup of the logo, with the icon above the wordmark. Suited to portrait / square brand-mark slots such as some cover layouts and section dividers. Falls back to 'logo' when not set. Accepts an HTTPS URL, data URI, relative path, or asset reference.",
          "examples": [
            "https://cdn.acme.com/logo-stacked.svg",
            "./assets/logo-stacked.png",
            "asset_brand_logo_stacked"
          ]
        },
        "logoStackedLight": {
          "type": "string",
          "description": "Light-colored stacked logo variant (e.g., white SVG) intended for rendering on dark backgrounds.",
          "examples": [
            "https://cdn.acme.com/logo-stacked-white.svg",
            "./assets/logo-stacked-white.png",
            "asset_brand_logo_stacked_light"
          ]
        },
        "logoStackedDark": {
          "type": "string",
          "description": "Dark-colored stacked logo variant (e.g., black SVG) intended for rendering on light backgrounds.",
          "examples": [
            "https://cdn.acme.com/logo-stacked-black.svg",
            "./assets/logo-stacked-black.png",
            "asset_brand_logo_stacked_dark"
          ]
        },
        "icon": {
          "type": "string",
          "description": "Default icon / mark / symbol (no wordmark). Useful for tight spaces such as footers, badges, and slide-corner marks. Accepts an HTTPS URL, data URI, relative path, or asset reference.",
          "examples": [
            "https://cdn.acme.com/icon.svg",
            "./assets/icon.png",
            "asset_brand_icon"
          ]
        },
        "iconLight": {
          "type": "string",
          "description": "Light-colored icon variant (e.g., white SVG) intended for rendering on dark backgrounds.",
          "examples": [
            "https://cdn.acme.com/icon-white.svg",
            "./assets/icon-white.png",
            "asset_brand_icon_light"
          ]
        },
        "iconDark": {
          "type": "string",
          "description": "Dark-colored icon variant (e.g., black SVG) intended for rendering on light backgrounds.",
          "examples": [
            "https://cdn.acme.com/icon-black.svg",
            "./assets/icon-black.png",
            "asset_brand_icon_dark"
          ]
        },
        "wordmark": {
          "type": "string",
          "description": "Default wordmark — the company name set in its branded typography, with no icon. Often used on cover and section-divider slides. Accepts an HTTPS URL, data URI, relative path, or asset reference.",
          "examples": [
            "https://cdn.acme.com/wordmark.svg",
            "./assets/wordmark.png",
            "asset_brand_wordmark"
          ]
        },
        "wordmarkLight": {
          "type": "string",
          "description": "Light-colored wordmark variant (e.g., white SVG) intended for rendering on dark backgrounds.",
          "examples": [
            "https://cdn.acme.com/wordmark-white.svg",
            "./assets/wordmark-white.png",
            "asset_brand_wordmark_light"
          ]
        },
        "wordmarkDark": {
          "type": "string",
          "description": "Dark-colored wordmark variant (e.g., black SVG) intended for rendering on light backgrounds.",
          "examples": [
            "https://cdn.acme.com/wordmark-black.svg",
            "./assets/wordmark-black.png",
            "asset_brand_wordmark_dark"
          ]
        },
        "watermark": {
          "$ref": "#/$defs/BrandWatermark",
          "description": "Default decorative watermark applied across slides. Used as the fallback when a themed (Light/Dark) variant isn't set."
        },
        "watermarkLight": {
          "$ref": "#/$defs/BrandWatermark",
          "description": "Light-colored watermark variant intended for rendering on dark backgrounds."
        },
        "watermarkDark": {
          "$ref": "#/$defs/BrandWatermark",
          "description": "Dark-colored watermark variant intended for rendering on light backgrounds."
        }
      }
    },
    "BrandWatermark": {
      "type": "object",
      "description": "Decorative watermark image and its rendering opacity. Reused by Brand.watermark, Brand.watermarkLight, and Brand.watermarkDark.",
      "properties": {
        "src": {
          "type": "string",
          "description": "Source for the watermark image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference.",
          "examples": [
            "https://cdn.acme.com/watermark.png",
            "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAusB9V3iWggAAAAASUVORK5CYII=",
            "./assets/watermark.png",
            "asset_brand_watermark"
          ]
        },
        "opacity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Watermark opacity from 0 (fully transparent) to 1 (fully opaque)."
        }
      }
    },
    "LayoutPreferences": {
      "type": "object",
      "description": "Hints that influence default layout decisions across slides.",
      "properties": {
        "density": {
          "type": "string",
          "enum": ["minimal", "balanced", "dense"],
          "description": "Default content density across slides."
        },
        "alignment": {
          "type": "string",
          "enum": ["left", "center", "right"],
          "description": "Default horizontal alignment for slide content."
        },
        "animations": {
          "type": "boolean",
          "description": "Whether to enable element animations by default."
        },
        "slideNumbers": {
          "type": "boolean",
          "description": "Whether to render slide numbers."
        }
      }
    },
    "Slide": {
      "type": "object",
      "description": "A single slide with its layout, elements, and presentation behavior.",
      "required": ["id", "layout", "elements"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the slide within the document.",
          "examples": ["s1", "cover", "problem-1", "ask"]
        },
        "layout": {
          "type": "string",
          "description": "Catalog reference resolved against catalogs.layouts and the default catalog at https://www.pptx.gallery/layouts. Accepts a bare id (lowercase kebab-case, e.g. 'title-left'), an HTTPS URL pointing at a record file, or a 'pkg:' reference to a locally-installed package. Free-form custom layout names that don't resolve through any catalog fall through to engine-defined layouts.\n\nResolution order: inline catalogs.layouts.records[] → catalogs.layouts.source → default catalog.",
          "examples": ["title-slide", "title-left", "section-divider", "two-column", "image-right", "stat-hero", "blank", "https://acme.com/decks/layouts/cover.json", "pkg:@acme/decks/layouts/cover"]
        },
        "designOverrides": {
          "$ref": "#/$defs/Design",
          "description": "Slide-level design overrides applied on top of the deck-wide design."
        },
        "elements": {
          "type": "array",
          "items": { "$ref": "#/$defs/Element" },
          "description": "Content elements rendered on this slide."
        },
        "notes": {
          "type": "string",
          "description": "Speaker notes shown in presenter view.",
          "examples": ["Open with the customer story; pause for questions before moving to the architecture slide."]
        },
        "transition": {
          "$ref": "#/$defs/Transition",
          "description": "Transition used when entering this slide."
        },
        "hidden": {
          "type": "boolean",
          "description": "Whether the slide is hidden from the presented sequence."
        },
        "section": {
          "type": "string",
          "description": "Section marker used to group slides in presenter view and outlines.",
          "examples": ["Intro", "Problem", "Solution", "Ask", "Appendix"]
        },
        "beat": {
          "oneOf": [
            { "type": "string" },
            {
              "type": "array",
              "items": { "type": "string" }
            }
          ],
          "description": "Optional reference to one or more narrative beats (each value matches an id from meta.narrative.beats or the resolved template). A single string declares the slide's primary beat; an array declares that one slide covers multiple beats — useful when shorter decks fold beats together (e.g. ['problem', 'why-now']). Declares the slide's role in the story arc; does not constrain ordering or structure. Slides without a beat are valid; multiple slides may share a beat."
        }
      }
    },
    "Transition": {
      "type": "object",
      "description": "Animated transition used when entering a slide.",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["none", "fade", "slide", "push", "wipe", "morph", "zoom"],
          "description": "Transition style."
        },
        "duration": {
          "type": "number",
          "description": "Transition duration in seconds."
        },
        "direction": {
          "type": "string",
          "enum": ["left", "right", "up", "down"],
          "description": "Direction for directional transitions (e.g., slide, push, wipe)."
        }
      }
    },
    "Position": {
      "type": "object",
      "description": "Element position on the slide.",
      "properties": {
        "x": {
          "type": "number",
          "description": "Horizontal coordinate; interpretation depends on the containing element's units."
        },
        "y": {
          "type": "number",
          "description": "Vertical coordinate; interpretation depends on the containing element's units."
        },
        "anchor": {
          "type": "string",
          "description": "Anchor to layout grid.",
          "examples": ["top-left", "top-right", "center", "bottom-left", "bottom-right"]
        }
      }
    },
    "Size": {
      "type": "object",
      "description": "Element size on the slide.",
      "properties": {
        "width": {
          "type": "number",
          "description": "Element width expressed in the unit specified by 'unit'."
        },
        "height": {
          "type": "number",
          "description": "Element height expressed in the unit specified by 'unit'."
        },
        "unit": {
          "type": "string",
          "enum": ["in", "%", "px"],
          "description": "Unit for width and height: 'in' (inches), '%' (percentage of slide), or 'px' (pixels)."
        }
      }
    },
    "Element": {
      "type": "object",
      "description": "A renderable item on a slide. The 'type' discriminator selects which additional fields are valid.",
      "required": ["id", "type"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the element within its slide.",
          "examples": ["title", "subtitle", "h1", "body-1", "logo", "chart"]
        },
        "type": {
          "type": "string",
          "enum": ["text", "image", "shape", "chart", "table", "video", "code", "group", "placeholder"],
          "description": "Element kind, which determines the additional fields available."
        },
        "position": {
          "$ref": "#/$defs/Position",
          "description": "Explicit element position; when omitted, the element is positioned by the layout."
        },
        "size": {
          "$ref": "#/$defs/Size",
          "description": "Explicit element size; when omitted, the element is sized by the layout."
        },
        "slot": {
          "type": "string",
          "description": "Named slot from the layout template.",
          "examples": ["title", "subtitle", "body", "image-right", "footer"]
        },
        "animation": {
          "$ref": "#/$defs/Animation",
          "description": "Animation applied to this element."
        }
      },
      "allOf": [
        {
          "if": { "properties": { "type": { "const": "text" } } },
          "then": {
            "properties": {
              "content": {
                "$ref": "#/$defs/TextContent",
                "description": "Text content as plain text, formatted runs, markdown, or list items."
              },
              "style": {
                "$ref": "#/$defs/TextStyle",
                "description": "Text styling applied to the entire content."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "image" } } },
          "then": {
            "required": ["src"],
            "properties": {
              "src": {
                "type": "string",
                "description": "Source for the image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference.",
                "examples": [
                  "https://cdn.acme.com/images/diagram.png",
                  "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAusB9V3iWggAAAAASUVORK5CYII=",
                  "./images/diagram.png",
                  "asset_img_diagram"
                ]
              },
              "alt": {
                "type": "string",
                "description": "Accessible alt text describing the image.",
                "examples": ["Architecture diagram showing the agent runtime", "Headshot of Alice Chen"]
              },
              "fit": {
                "type": "string",
                "enum": ["cover", "contain", "fill", "none"],
                "description": "How the image fills its bounding box."
              },
              "borderRadius": {
                "type": "number",
                "description": "Corner radius in points; produces rounded corners when greater than 0."
              },
              "shadow-sm": {
                "type": "boolean",
                "description": "Whether to render a small drop shadow behind the image."
              },
              "caption": {
                "type": "string",
                "description": "Caption rendered beneath the image.",
                "examples": ["Source: Q3 2026 internal benchmarks"]
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "chart" } } },
          "then": {
            "required": ["data"],
            "anyOf": [
              { "required": ["chartType"] },
              { "required": ["chartPresetId"] }
            ],
            "properties": {
              "chartType": {
                "type": "string",
                "description": "Generic chart variant. Use this for engine-agnostic decks where you want the engine to pick a sensible preset; for finer control, use 'chartPresetId' instead. Accepts a generic type ('bar', 'line', etc.) or any chart-preset id from /spec/charts (the engine treats common preset ids as synonyms here).",
                "examples": [
                  "bar", "column", "line", "pie", "donut", "doughnut", "area", "scatter", "radar",
                  "waterfall", "funnel", "treemap", "combo",
                  "clustered-column", "stacked-column", "stacked-column-100", "bullet-column", "box-and-whisker", "histogram"
                ]
              },
              "chartPresetId": {
                "type": "string",
                "description": "Catalog reference resolved against catalogs.charts and the default catalog at https://www.pptx.gallery/charts. Accepts a bare id (lowercase kebab-case, e.g. 'clustered-column'), an HTTPS URL pointing at a record file, or a 'pkg:' reference to a locally-installed package. When set, the engine sources the visual template from the resolved preset and uses 'data' to populate it; 'chartType' (if also set) is treated as a generic-class hint.\n\nResolution order: inline catalogs.charts.records[] → catalogs.charts.source → default catalog.",
                "examples": ["column", "clustered-column", "stacked-column", "doughnut", "bullet-column", "box-and-whisker", "https://acme.com/decks/charts/branded-column.json", "pkg:@acme/decks/charts/branded-column"]
              },
              "data": {
                "$ref": "#/$defs/ChartData",
                "description": "Chart data, including labels and datasets."
              },
              "options": {
                "$ref": "#/$defs/ChartOptions",
                "description": "Visual and behavioral chart options."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "table" } } },
          "then": {
            "required": ["rows"],
            "properties": {
              "headers": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Header row labels rendered above the data rows.",
                "examples": [["Region", "Q3 Revenue", "Q4 Revenue", "YoY %"]]
              },
              "rows": {
                "type": "array",
                "items": { "type": "array" },
                "description": "Two-dimensional array of row data; each row aligns by index with the headers/columns.",
                "examples": [[["North America", 12.4, 18.1, 46], ["EMEA", 8.2, 11.5, 40]]]
              },
              "style": {
                "$ref": "#/$defs/TableStyle",
                "description": "Visual styling applied to the table."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "shape" } } },
          "then": {
            "required": ["shape"],
            "properties": {
              "shape": {
                "type": "string",
                "description": "Shape type.",
                "examples": ["rectangle", "circle", "ellipse", "triangle", "arrow", "line", "star", "callout", "custom"]
              },
              "fill": {
                "type": "string",
                "description": "Fill color as a hex string.",
                "examples": ["#1E40AF", "#F8FAFC", "#22D3EE"]
              },
              "stroke": {
                "type": "object",
                "description": "Outline stroke for the shape.",
                "properties": {
                  "color": {
                    "type": "string",
                    "description": "Stroke color as a hex string.",
                    "examples": ["#0F172A", "#FFFFFF"]
                  },
                  "width": {
                    "type": "number",
                    "description": "Stroke width in points."
                  }
                }
              },
              "cornerRadius": {
                "type": "number",
                "description": "Corner radius in points (used for rectangles)."
              },
              "text": {
                "$ref": "#/$defs/TextContent",
                "description": "Text rendered inside the shape."
              },
              "path": {
                "type": "string",
                "description": "SVG path data used when shape is 'custom'.",
                "examples": ["M10 10 L90 10 L50 90 Z"]
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "code" } } },
          "then": {
            "required": ["code"],
            "properties": {
              "code": {
                "type": "string",
                "description": "Source code to display.",
                "examples": ["def greet(name: str) -> str:\n    return f\"Hello, {name}!\""]
              },
              "language": {
                "type": "string",
                "description": "Language identifier used for syntax highlighting.",
                "examples": ["python", "ts", "tsx", "rust", "go", "bash", "json", "yaml"]
              },
              "theme": {
                "type": "string",
                "enum": ["dark", "light"],
                "description": "Syntax highlighting theme."
              },
              "showLineNumbers": {
                "type": "boolean",
                "description": "Whether to render line numbers alongside the code."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "group" } } },
          "then": {
            "required": ["children"],
            "properties": {
              "children": {
                "type": "array",
                "items": { "$ref": "#/$defs/Element" },
                "description": "Child elements rendered as a group; their positions and sizes nest within the group's bounds."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "placeholder" } } },
          "then": {
            "required": ["prompt"],
            "properties": {
              "prompt": {
                "type": "string",
                "description": "AI instruction describing what content should be generated for this slot.",
                "examples": [
                  "Three bullet points summarizing why our latency advantage matters to enterprise buyers.",
                  "A two-column comparison of our product vs. the leading competitor."
                ]
              },
              "expectedType": {
                "type": "string",
                "enum": ["text", "image", "chart", "table"],
                "description": "Expected content type the engine or agent should produce."
              }
            }
          }
        }
      ]
    },
    "Animation": {
      "type": "object",
      "description": "Animation effect applied to an element.",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["appear", "fadeIn", "slideIn", "zoomIn", "typewriter", "custom"],
          "description": "Animation effect applied to the element."
        },
        "delay": {
          "type": "number",
          "description": "Delay before the animation starts, in seconds."
        },
        "duration": {
          "type": "number",
          "description": "Animation duration in seconds."
        },
        "order": {
          "type": "integer",
          "description": "Order in the slide's animation sequence; lower values play first."
        }
      }
    },
    "TextContent": {
      "type": "object",
      "description": "Text content expressed as plain text, rich runs, markdown, or list items.",
      "properties": {
        "text": {
          "type": "string",
          "description": "Simple text string for basic use; ignored when 'runs' or 'markdown' is provided.",
          "examples": ["Hello, world", "The Future of Agentic Workflows"]
        },
        "runs": {
          "type": "array",
          "items": { "$ref": "#/$defs/TextRun" },
          "description": "Rich text expressed as formatted runs."
        },
        "markdown": {
          "type": "string",
          "description": "Markdown source converted to runs by the engine.",
          "examples": ["**Bold** opening followed by _emphasis_ and a [link](https://acme.com)."]
        },
        "bullets": {
          "type": "array",
          "description": "Bulleted list items; each item is either a string or a nested TextContent.",
          "examples": [["Sub-second agent latency", "Drop-in compatible with OpenAI tool-calling", "SOC 2 Type II"]]
        },
        "numbered": {
          "type": "array",
          "description": "Numbered list items; each item is either a string or a nested TextContent.",
          "examples": [["Identify the bottleneck", "Profile end-to-end", "Ship the fix"]]
        }
      }
    },
    "TextRun": {
      "type": "object",
      "description": "A contiguous run of text sharing the same character formatting.",
      "required": ["text"],
      "properties": {
        "text": {
          "type": "string",
          "description": "Text for this run.",
          "examples": ["Hello, ", "world"]
        },
        "bold": {
          "type": "boolean",
          "description": "Whether the run is rendered in bold."
        },
        "italic": {
          "type": "boolean",
          "description": "Whether the run is rendered in italic."
        },
        "underline": {
          "type": "boolean",
          "description": "Whether the run is underlined."
        },
        "strikethrough": {
          "type": "boolean",
          "description": "Whether the run is rendered with a strikethrough line."
        },
        "color": {
          "type": "string",
          "description": "Run text color as a hex string.",
          "examples": ["#0F172A", "#1E40AF"]
        },
        "fontSize": {
          "type": "number",
          "description": "Run font size in points."
        },
        "fontFamily": {
          "type": "string",
          "description": "Run font family override.",
          "examples": ["Inter", "Source Serif Pro", "JetBrains Mono"]
        },
        "link": {
          "type": "string",
          "description": "URL to link the run text to.",
          "examples": ["https://acme.com", "https://acme.com/case-studies/beta"]
        },
        "superscript": {
          "type": "boolean",
          "description": "Whether the run is rendered as superscript."
        },
        "subscript": {
          "type": "boolean",
          "description": "Whether the run is rendered as subscript."
        }
      }
    },
    "TextStyle": {
      "type": "object",
      "description": "Paragraph-level text styling applied to a TextContent.",
      "properties": {
        "fontSize": {
          "type": "number",
          "description": "Font size in points."
        },
        "fontFamily": {
          "type": "string",
          "description": "Font family override.",
          "examples": ["Inter", "Source Serif Pro", "JetBrains Mono"]
        },
        "color": {
          "type": "string",
          "description": "Text color as a hex string.",
          "examples": ["#0F172A", "#F8FAFC"]
        },
        "alignment": {
          "type": "string",
          "enum": ["left", "center", "right", "justify"],
          "description": "Horizontal text alignment."
        },
        "lineHeight": {
          "type": "number",
          "description": "Line height multiplier (e.g., 1.5)."
        },
        "letterSpacing": {
          "type": "number",
          "description": "Letter spacing (tracking) in ems."
        },
        "textTransform": {
          "type": "string",
          "enum": ["none", "uppercase", "lowercase", "capitalize"],
          "description": "Capitalization transform applied to the text."
        },
        "verticalAlignment": {
          "type": "string",
          "enum": ["top", "middle", "bottom"],
          "description": "Vertical alignment within the text frame."
        }
      }
    },
    "ChartData": {
      "type": "object",
      "description": "Data driving a chart element.",
      "required": ["datasets"],
      "properties": {
        "labels": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Category labels shared across datasets (e.g., x-axis tick labels).",
          "examples": [["Q1", "Q2", "Q3", "Q4"], ["Jan", "Feb", "Mar", "Apr", "May", "Jun"]]
        },
        "datasets": {
          "type": "array",
          "description": "One or more data series rendered on the chart.",
          "items": {
            "type": "object",
            "description": "A single data series.",
            "required": ["label", "values"],
            "properties": {
              "label": {
                "type": "string",
                "description": "Dataset name shown in the legend.",
                "examples": ["Revenue ($M)", "MAU", "Latency (ms)"]
              },
              "values": {
                "type": "array",
                "items": { "type": "number" },
                "description": "Numeric values for the dataset; aligns by index with the chart's labels.",
                "examples": [[12.4, 14.1, 16.8, 18.1]]
              },
              "color": {
                "type": "string",
                "description": "Override color for this dataset as a hex string.",
                "examples": ["#1E40AF", "#22D3EE"]
              },
              "type": {
                "type": "string",
                "description": "Per-dataset chart type override (used by combo charts).",
                "examples": ["line", "column", "area"]
              }
            }
          }
        }
      }
    },
    "ChartOptions": {
      "type": "object",
      "description": "Visual and behavioral options for a chart element.",
      "properties": {
        "title": {
          "type": "string",
          "description": "Chart title shown above the plot.",
          "examples": ["Quarterly Revenue", "Monthly Active Users (MAU)"]
        },
        "legend": {
          "description": "Legend configuration: pass a boolean to toggle the legend, or an object such as { position: 'top' | 'bottom' | 'left' | 'right' } to customize placement."
        },
        "showValues": {
          "type": "boolean",
          "description": "Whether to render data labels next to each value."
        },
        "showGrid": {
          "type": "boolean",
          "description": "Whether to render gridlines on the chart."
        },
        "stacked": {
          "type": "boolean",
          "description": "Whether series should be stacked rather than grouped."
        },
        "yAxis": {
          "type": "object",
          "description": "Y-axis configuration.",
          "properties": {
            "label": {
              "type": "string",
              "description": "Y-axis title.",
              "examples": ["Revenue ($M)", "Latency (ms)", "Users"]
            },
            "min": {
              "type": "number",
              "description": "Minimum y-axis value."
            },
            "max": {
              "type": "number",
              "description": "Maximum y-axis value."
            },
            "format": {
              "type": "string",
              "description": "Tick label format string.",
              "examples": ["$#,##0", "0.0%", "#,##0", "0.0\"ms\""]
            }
          }
        },
        "xAxis": {
          "type": "object",
          "description": "X-axis configuration.",
          "properties": {
            "label": {
              "type": "string",
              "description": "X-axis title.",
              "examples": ["Quarter", "Month", "Region"]
            },
            "format": {
              "type": "string",
              "description": "Tick label format string.",
              "examples": ["mmm yyyy", "0"]
            }
          }
        }
      }
    },
    "TableStyle": {
      "type": "object",
      "description": "Visual styling applied to a table element.",
      "properties": {
        "headerBackground": {
          "type": "string",
          "description": "Header row background color as a hex string.",
          "examples": ["#0F172A", "#1E40AF"]
        },
        "headerColor": {
          "type": "string",
          "description": "Header row text color as a hex string.",
          "examples": ["#FFFFFF", "#F8FAFC"]
        },
        "stripedRows": {
          "type": "boolean",
          "description": "Whether to alternate row backgrounds for readability."
        },
        "borderColor": {
          "type": "string",
          "description": "Color of cell borders as a hex string.",
          "examples": ["#E2E8F0", "#475569"]
        },
        "compact": {
          "type": "boolean",
          "description": "Use a denser row height and reduced padding."
        }
      }
    },
    "Catalogs": {
      "type": "object",
      "description": "Catalog overrides for the in-document references. Every property is optional. The default catalog for a kind lives at https://www.pptx.gallery/<kind> (e.g. https://www.pptx.gallery/narratives, https://www.pptx.gallery/themes). For each kind, declaring a 'source' replaces the default registry and/or 'records' adds inline records that take precedence over anything fetched from a source.\n\nResolution order for any reference (e.g. meta.narrative.template, design.theme): inline catalogs.<kind>.records[] → catalogs.<kind>.source → default catalog at https://www.pptx.gallery/<kind>. References that are themselves URLs or 'pkg:' references skip the catalog lookup and resolve directly.",
      "properties": {
        "narratives": {
          "$ref": "#/$defs/CatalogEntry",
          "description": "Catalog of narrative templates. Records validate against https://pptx.dev/schema/opf-narrative/v1. Default source: https://www.pptx.gallery/narratives."
        },
        "themes": {
          "$ref": "#/$defs/CatalogEntry",
          "description": "Catalog of themes. Records validate against https://pptx.dev/schema/opf-theme/v1. Default source: https://www.pptx.gallery/themes."
        },
        "colorSchemes": {
          "$ref": "#/$defs/CatalogEntry",
          "description": "Catalog of color schemes. Records validate against https://pptx.dev/schema/opf-color-scheme/v1. Default source: https://www.pptx.gallery/color-schemes."
        },
        "fontSchemes": {
          "$ref": "#/$defs/CatalogEntry",
          "description": "Catalog of font schemes. Records validate against https://pptx.dev/schema/opf-font-scheme/v1. Default source: https://www.pptx.gallery/font-schemes."
        },
        "languages": {
          "$ref": "#/$defs/CatalogEntry",
          "description": "Catalog of languages. Records validate against https://pptx.dev/schema/opf-language/v1. Default source: https://www.pptx.gallery/languages."
        },
        "layouts": {
          "$ref": "#/$defs/CatalogEntry",
          "description": "Catalog of slide layouts. Records validate against https://pptx.dev/schema/opf-layout/v1. Default source: https://www.pptx.gallery/layouts."
        },
        "charts": {
          "$ref": "#/$defs/CatalogEntry",
          "description": "Catalog of chart presets. Records validate against https://pptx.dev/schema/opf-chart/v1. Default source: https://www.pptx.gallery/charts."
        }
      }
    },
    "CatalogEntry": {
      "type": "object",
      "description": "A catalog override for one record kind. 'source' replaces the default registry; 'records' adds inline records that take precedence over anything fetched from a source. Either or both may be provided; both omitted means the kind uses its default catalog.",
      "properties": {
        "source": {
          "oneOf": [
            { "$ref": "#/$defs/CatalogSource" },
            {
              "type": "array",
              "items": { "$ref": "#/$defs/CatalogSource" },
              "minItems": 1,
              "description": "Ordered search path of sources. The engine tries each in order; first match wins. The default catalog for the kind is appended implicitly at the end."
            }
          ],
          "description": "Single source or an ordered search path of sources. When omitted, the engine falls back to https://www.pptx.gallery/<kind>."
        },
        "records": {
          "type": "array",
          "items": { "type": "object" },
          "description": "Inline catalog records embedded in this OPF document. Each record validates against the kind's companion schema (e.g. https://pptx.dev/schema/opf-narrative/v1 for narratives). Inline records win over anything resolved from 'source' and over the default catalog."
        }
      }
    },
    "CatalogSource": {
      "type": "string",
      "description": "Catalog source location. Accepts:\n- A bare URL pointing at a catalog directory (e.g. 'https://acme.com/decks/narratives'); record ids resolve to '<base>/<id>.json'.\n- A URL pointing at an index file (e.g. 'https://acme.com/decks/narratives/index.json'); records are resolved relative to the index file's directory and the index entries describe what's available.\n- A package reference of the form 'pkg:<package>[/<subpath>]'; resolved through a locally-installed package on the engine's package path.",
      "examples": [
        "https://www.pptx.gallery/narratives",
        "https://acme.com/decks/narratives/index.json",
        "pkg:@acme/decks/narratives",
        "pkg:pptx-gallery"
      ]
    }
  }
}
