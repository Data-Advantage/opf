{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pptx.dev/schema/opf/v1",
  "title": "OPF — Open Presentation Format",
  "description": "Open-source JSON format for describing PowerPoint presentations. Agents describe narratives, content, audience, and design — the engine handles OOXML complexity.",
  "type": "object",
  "required": ["$schema", "meta", "design", "slides"],
  "properties": {
    "$schema": {
      "type": "string",
      "const": "https://pptx.dev/schema/opf/v1",
      "description": "Identifies the OPF schema version so validators and editors can resolve the correct rules."
    },
    "meta": {
      "$ref": "#/$defs/Meta",
      "description": "High-level presentation metadata such as title, speakers, organizations, audience, and narrative."
    },
    "design": {
      "$ref": "#/$defs/Design",
      "description": "Design system covering colors, fonts, dimensions, background, brand, and layout preferences applied to the deck."
    },
    "slides": {
      "type": "array",
      "items": { "$ref": "#/$defs/Slide" },
      "minItems": 1,
      "description": "Ordered array of slides that make up the presentation."
    },
    "extensions": {
      "type": "object",
      "description": "Custom data passthrough for agent workflows; ignored by the engine but preserved across read/write round-trips."
    }
  },
  "$defs": {
    "Meta": {
      "type": "object",
      "description": "Presentation-level metadata describing what the deck is, who it's for, and how it should be told.",
      "required": ["title"],
      "properties": {
        "title": {
          "type": "string",
          "description": "Display title of the presentation."
        },
        "description": {
          "type": "string",
          "description": "Free-form prose describing what this presentation is about. Used by agents and humans as a deck-level summary; complements meta.purpose (the goal) and meta.narrative (the structured storyline)."
        },
        "filename": {
          "type": "string",
          "description": "Optional base filename for exports (without extension). Engine strips a trailing .pptx, .pdf, .png, or .svg (case-insensitive) and appends the target format's extension. When omitted, the engine slugifies meta.title. See /docs/opf for full validation rules."
        },
        "subtitle": {
          "type": "string",
          "description": "Optional subtitle shown alongside the title, typically on cover slides."
        },
        "organizations": {
          "type": "array",
          "items": { "$ref": "#/$defs/Organization" },
          "description": "Organizations associated with the presentation (e.g., the presenting company, host conference, partners, clients, sponsors). The 'primary' organization (declared via Organization.role or, if no role is set, the first item) drives default branding such as cover-slide logo. Optional — simple decks may omit this."
        },
        "speakers": {
          "type": "array",
          "items": { "$ref": "#/$defs/Speaker" },
          "description": "People presenting the deck. Used for cover slides, bio slides, footers, and panel attribution. Optional — simple decks may omit this."
        },
        "authors": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Optional credit list for people who authored or contributed to the deck, distinct from speakers. Use when the writer and presenter differ (e.g., analyst-written, exec-presented)."
        },
        "audience": {
          "type": "string",
          "description": "Intended audience for the presentation (e.g., 'executives', 'engineering team'). Used by AI to tune tone and depth."
        },
        "purpose": {
          "type": "string",
          "description": "Primary goal of the presentation (e.g., 'pitch for Series B funding'). Complements meta.description and meta.narrative."
        },
        "narrative": {
          "$ref": "#/$defs/Narrative",
          "description": "Structured storyline describing the deck's arc, key messages, and tone."
        },
        "language": {
          "type": "string",
          "description": "BCP 47 language tag for the presentation content (e.g., 'en-US')."
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Free-form labels used for categorization, search, and filtering. Lowercase kebab-case is recommended for consistency across a deck library."
        }
      }
    },
    "Organization": {
      "type": "object",
      "description": "An organization associated with the presentation — typically the presenting company, but also hosts, partners, clients, or sponsors. Surfaced on cover slides, footers, and brand bars; the primary organization's logo is the default for design.brand.logo unless overridden.",
      "required": ["id", "name"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-zA-Z0-9_-]+$",
          "description": "Stable identifier for the organization, used to reference it from Speaker.organizationId. Must be unique within the deck."
        },
        "name": {
          "type": "string",
          "description": "Display name shown on slides (e.g., 'Acme Corp')."
        },
        "legalName": {
          "type": "string",
          "description": "Optional legal entity name when it differs from the display name (e.g., 'Acme Corporation, Inc.')."
        },
        "logo": {
          "type": "string",
          "description": "Source for the organization's logo image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference. The primary organization's logo is used by default for cover-slide and footer branding; design.brand.logo overrides both source and rendering."
        },
        "domain": {
          "type": "string",
          "description": "Bare internet domain for the organization (e.g., 'acme.com'). Used for footers, contact slides, and engine-driven asset lookups (e.g., favicon-based brand defaults)."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "General contact email for the organization (e.g., 'hello@acme.com'). Used on contact slides and footer attribution."
        },
        "phone": {
          "type": "string",
          "description": "Main contact phone number for the organization. E.164 format is recommended (e.g., '+14155551234')."
        },
        "tagline": {
          "type": "string",
          "description": "Short tagline rendered alongside the organization name on cover slides."
        },
        "role": {
          "type": "string",
          "enum": ["primary", "partner", "client", "sponsor", "host"],
          "description": "Role of the organization relative to the presentation. When omitted, the first organization in the array is treated as primary."
        },
        "socials": {
          "$ref": "#/$defs/Socials",
          "description": "Optional social media handles or URLs for the organization."
        }
      }
    },
    "Speaker": {
      "type": "object",
      "description": "A person presenting the deck. Used for cover slides, bio/intro slides, footer attribution, and panel formats with multiple presenters.",
      "required": ["id", "name"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-zA-Z0-9_-]+$",
          "description": "Stable identifier for the speaker, used for cross-references within the deck. Must be unique within the deck."
        },
        "name": {
          "type": "string",
          "description": "Display name (e.g., 'Alice Chen')."
        },
        "title": {
          "type": "string",
          "description": "Role or title (e.g., 'VP of Engineering', 'Founder & CEO'). Often paired with the speaker's organization on cover slides."
        },
        "photo": {
          "type": "string",
          "description": "URL or asset reference for the speaker's headshot, used on cover and bio slides."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "Contact email, used on contact slides or footer attribution when appropriate."
        },
        "phone": {
          "type": "string",
          "description": "Contact phone number for the speaker. E.164 format is recommended (e.g., '+14155551234')."
        },
        "bio": {
          "type": "string",
          "description": "Short biographical paragraph for bio or 'about the speaker' slides."
        },
        "organizationId": {
          "type": "string",
          "description": "Reference to an Organization.id in meta.organizations. Lets a speaker be attributed to their org in panel or multi-org decks without repeating organization details."
        },
        "socials": {
          "$ref": "#/$defs/Socials",
          "description": "Optional social media handles or URLs for the speaker."
        }
      }
    },
    "Socials": {
      "type": "object",
      "description": "Social media handles or URLs. Every property is optional; consumers may render values either as full URLs or as platform handles (e.g., '@acme'). Use this both for organizations and individual speakers.",
      "properties": {
        "linkedin": {
          "type": "string",
          "description": "LinkedIn profile or company page URL or handle.",
          "examples": ["https://linkedin.com/in/alice-chen", "https://linkedin.com/company/acme"]
        },
        "x": {
          "type": "string",
          "description": "X (formerly Twitter) profile URL or handle.",
          "examples": ["@alicechen", "https://x.com/acme"]
        },
        "github": {
          "type": "string",
          "description": "GitHub user or organization URL or handle.",
          "examples": ["alicechen", "https://github.com/acme"]
        },
        "youtube": {
          "type": "string",
          "description": "YouTube channel URL or handle.",
          "examples": ["@acme", "https://youtube.com/@acme"]
        },
        "instagram": {
          "type": "string",
          "description": "Instagram profile URL or handle.",
          "examples": ["@acme", "https://instagram.com/acme"]
        },
        "facebook": {
          "type": "string",
          "description": "Facebook page URL or handle.",
          "examples": ["acme", "https://facebook.com/acme"]
        },
        "tiktok": {
          "type": "string",
          "description": "TikTok profile URL or handle.",
          "examples": ["@acme", "https://tiktok.com/@acme"]
        },
        "threads": {
          "type": "string",
          "description": "Threads profile URL or handle.",
          "examples": ["@alicechen", "https://threads.net/@acme"]
        },
        "mastodon": {
          "type": "string",
          "description": "Mastodon profile URL (include the instance, e.g., 'https://mastodon.social/@user').",
          "examples": ["https://mastodon.social/@alice", "https://hachyderm.io/@acme"]
        },
        "bluesky": {
          "type": "string",
          "description": "Bluesky profile URL or handle (e.g., 'user.bsky.social').",
          "examples": ["alice.bsky.social", "https://bsky.app/profile/acme.bsky.social"]
        }
      }
    },
    "Narrative": {
      "type": "object",
      "description": "Structured storyline metadata used by AI to shape generated content. Narrative declares the deck's intended story arc; slides may opt into beats via Slide.beat. The narrative does not constrain slide structure — validators warn on drift (orphan slides, unused beats) but never error. Slides are the source of truth; narrative is intent that travels with the deck.",
      "properties": {
        "template": {
          "type": "string",
          "description": "Reference to a named narrative template from the pptx.dev library, e.g. 'problem-solution', 'scqa', 'pitch-deck', 'qbr'. The library lives at /spec/narratives. Unknown template IDs produce a validation warning, not an error."
        },
        "description": {
          "type": "string",
          "description": "Free-form narrative description used for AI-driven generation."
        },
        "keyMessages": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Key messages the presentation should convey."
        },
        "tone": {
          "type": "string",
          "enum": ["formal", "casual", "inspirational", "technical", "persuasive"],
          "description": "Desired tone for the presentation."
        },
        "durationMinutes": {
          "type": "number",
          "minimum": 1,
          "description": "Target presentation duration in minutes."
        },
        "beats": {
          "type": "array",
          "items": { "$ref": "#/$defs/NarrativeBeat" },
          "description": "Inline narrative beats. When provided alongside a 'template', beats here override or extend matching template beats by 'id'. Without 'template', this defines a fully custom narrative arc. Beat IDs must be unique within the document."
        }
      }
    },
    "NarrativeBeat": {
      "type": "object",
      "description": "A single narrative beat — a labeled segment of the story arc with a specific dramatic purpose (e.g. 'hook', 'problem', 'evidence', 'ask'). Slides reference beats via Slide.beat.",
      "required": ["id", "name"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Stable slug used by Slide.beat to reference this beat. Lowercase kebab-case."
        },
        "name": {
          "type": "string",
          "description": "Human-readable beat name, e.g. 'The Problem'."
        },
        "description": {
          "type": "string",
          "description": "What this beat should accomplish. Used as a prompt for AI-driven generation of slides assigned to this beat."
        },
        "slideCount": {
          "type": "integer",
          "minimum": 1,
          "description": "Optional explicit slide count for this beat. Defaults to 1 when omitted; values >1 are reserved for beats that intentionally span multiple slides. Prefer decomposing a heavy beat into multiple beats over setting a high slideCount. The validator emits a warning if the deck's actual count differs significantly."
        },
        "layoutHint": {
          "type": "string",
          "description": "Suggested layout for the opening slide of this beat, e.g. 'section-divider', 'title-slide'."
        }
      }
    },
    "Design": {
      "type": "object",
      "description": "Visual design system applied to the presentation; individual slides may override fields via Slide.designOverrides.",
      "properties": {
        "theme": {
          "type": "string",
          "description": "Reference a named theme from pptx.gallery, or define the design inline via the other fields."
        },
        "colors": {
          "$ref": "#/$defs/ColorScheme",
          "description": "Color palette for the presentation."
        },
        "fonts": {
          "$ref": "#/$defs/FontScheme",
          "description": "Typography selections for heading, body, accent, and code text."
        },
        "dimensions": {
          "$ref": "#/$defs/Dimensions",
          "description": "Slide dimensions and aspect ratio."
        },
        "background": {
          "$ref": "#/$defs/Background",
          "description": "Default slide background applied across the deck unless overridden on a slide."
        },
        "brand": {
          "$ref": "#/$defs/Brand",
          "description": "Visual brand assets applied to slides (watermark, and an optional logo override). Organization identity (name, primary logo) lives in meta.organizations; design.brand only overrides visual rendering."
        },
        "layoutPreferences": {
          "$ref": "#/$defs/LayoutPreferences",
          "description": "Hints that influence layout decisions across slides."
        }
      }
    },
    "ColorScheme": {
      "type": "object",
      "description": "Color palette used by the design system. Pick a named scheme and/or override individual color roles.",
      "properties": {
        "scheme": {
          "type": "string",
          "description": "Named color scheme from the gallery, e.g. 'ocean-depth' or 'corporate-blue'."
        },
        "primary": {
          "type": "string",
          "description": "Primary brand color as a hex string (e.g., '#1E40AF')."
        },
        "secondary": {
          "type": "string",
          "description": "Secondary brand color as a hex string."
        },
        "accent": {
          "type": "string",
          "description": "Accent color used for highlights and emphasis."
        },
        "background": {
          "type": "string",
          "description": "Default slide background color as a hex string."
        },
        "surface": {
          "type": "string",
          "description": "Color for elevated surfaces such as cards and panels."
        },
        "text": {
          "type": "string",
          "description": "Primary body text color."
        },
        "textSecondary": {
          "type": "string",
          "description": "Secondary or muted text color used for captions and supporting copy."
        },
        "custom": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Map of custom named colors for advanced or theme-specific use."
        }
      }
    },
    "FontScheme": {
      "type": "object",
      "description": "Typography selections used by the design system. Pick a named scheme and/or override individual font roles.",
      "properties": {
        "scheme": {
          "type": "string",
          "description": "Named font scheme from the gallery, e.g. 'modern-sans' or 'classic-serif'."
        },
        "heading": {
          "$ref": "#/$defs/Font",
          "description": "Font used for slide titles and headings."
        },
        "body": {
          "$ref": "#/$defs/Font",
          "description": "Font used for body copy."
        },
        "accent": {
          "$ref": "#/$defs/Font",
          "description": "Font used for accent text such as quotes or callouts."
        },
        "code": {
          "$ref": "#/$defs/Font",
          "description": "Monospaced font used for code blocks."
        }
      }
    },
    "Font": {
      "type": "object",
      "description": "Specification for a single font role.",
      "required": ["family"],
      "properties": {
        "family": {
          "type": "string",
          "description": "Font family name (e.g., 'Inter', 'Roboto')."
        },
        "weight": {
          "type": "number",
          "description": "Numeric font weight (e.g., 400 for regular, 700 for bold)."
        },
        "style": {
          "type": "string",
          "enum": ["normal", "italic"],
          "description": "Font style."
        },
        "letterSpacing": {
          "type": "number",
          "description": "Letter spacing (tracking) in ems."
        }
      }
    },
    "Dimensions": {
      "type": "object",
      "description": "Slide dimensions; either pick a preset or specify custom inches.",
      "properties": {
        "preset": {
          "type": "string",
          "enum": ["16:9", "4:3", "16:10", "letter", "a4"],
          "description": "Named dimension preset; chooses both aspect ratio and physical size."
        },
        "widthInches": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Custom slide width in inches; overrides the preset width when provided."
        },
        "heightInches": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Custom slide height in inches; overrides the preset height when provided."
        }
      }
    },
    "Background": {
      "type": "object",
      "description": "Background fill applied to slides.",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["solid", "gradient", "image", "pattern"],
          "description": "Background fill type."
        },
        "color": {
          "type": "string",
          "description": "Solid fill color as a hex string (used when type is 'solid')."
        },
        "gradient": {
          "type": "object",
          "description": "Gradient fill definition (used when type is 'gradient').",
          "properties": {
            "angle": {
              "type": "number",
              "description": "Gradient angle in degrees (0 = left-to-right, 90 = top-to-bottom)."
            },
            "stops": {
              "type": "array",
              "description": "Ordered list of color stops along the gradient.",
              "items": {
                "type": "object",
                "description": "Color stop along the gradient.",
                "required": ["color", "position"],
                "properties": {
                  "color": {
                    "type": "string",
                    "description": "Stop color as a hex string."
                  },
                  "position": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Stop position along the gradient, from 0 (start) to 1 (end)."
                  }
                }
              }
            }
          }
        },
        "image": {
          "type": "object",
          "description": "Image fill definition (used when type is 'image').",
          "required": ["src"],
          "properties": {
            "src": {
              "type": "string",
              "description": "Source for the background image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference."
            },
            "fit": {
              "type": "string",
              "enum": ["cover", "contain", "tile"],
              "description": "How the image fills the slide background."
            }
          }
        },
        "opacity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Background opacity from 0 (fully transparent) to 1 (fully opaque)."
        }
      }
    },
    "Brand": {
      "type": "object",
      "description": "Visual brand assets surfaced across slides. Organization identity (name, primary logo) lives in meta.organizations; this object only carries visual overrides and decorative marks.",
      "properties": {
        "logo": {
          "type": "object",
          "description": "Optional logo override. When set, this takes precedence over the primary organization's logo for slide-level rendering. When omitted, the engine falls back to meta.organizations[primary].logo and uses default placement and sizing.",
          "properties": {
            "src": {
              "type": "string",
              "description": "Source for the logo image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference."
            },
            "position": {
              "$ref": "#/$defs/Position",
              "description": "Where the logo is placed on the slide."
            },
            "widthInches": {
              "type": "number",
              "description": "Rendered logo width in inches."
            }
          }
        },
        "watermark": {
          "type": "object",
          "description": "Watermark image displayed across slides.",
          "properties": {
            "src": {
              "type": "string",
              "description": "Source for the watermark image. Accepts an HTTPS URL, data URI, relative path (resolved against the OPF file location), or asset reference."
            },
            "opacity": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "Watermark opacity from 0 (fully transparent) to 1 (fully opaque)."
            }
          }
        }
      }
    },
    "LayoutPreferences": {
      "type": "object",
      "description": "Hints that influence default layout decisions across slides.",
      "properties": {
        "density": {
          "type": "string",
          "enum": ["minimal", "balanced", "dense"],
          "description": "Default content density across slides."
        },
        "alignment": {
          "type": "string",
          "enum": ["left", "center", "right"],
          "description": "Default horizontal alignment for slide content."
        },
        "animations": {
          "type": "boolean",
          "description": "Whether to enable element animations by default."
        },
        "slideNumbers": {
          "type": "boolean",
          "description": "Whether to render slide numbers."
        }
      }
    },
    "Slide": {
      "type": "object",
      "description": "A single slide with its layout, elements, and presentation behavior.",
      "required": ["id", "layout", "elements"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the slide within the document."
        },
        "layout": {
          "type": "string",
          "description": "Layout type — references a pptx.gallery layout or a custom layout name."
        },
        "designOverrides": {
          "$ref": "#/$defs/Design",
          "description": "Slide-level design overrides applied on top of the deck-wide design."
        },
        "elements": {
          "type": "array",
          "items": { "$ref": "#/$defs/Element" },
          "description": "Content elements rendered on this slide."
        },
        "notes": {
          "type": "string",
          "description": "Speaker notes shown in presenter view."
        },
        "transition": {
          "$ref": "#/$defs/Transition",
          "description": "Transition used when entering this slide."
        },
        "hidden": {
          "type": "boolean",
          "description": "Whether the slide is hidden from the presented sequence."
        },
        "section": {
          "type": "string",
          "description": "Section marker used to group slides in presenter view and outlines."
        },
        "beat": {
          "oneOf": [
            { "type": "string" },
            {
              "type": "array",
              "items": { "type": "string" }
            }
          ],
          "description": "Optional reference to one or more narrative beats (each value matches an id from meta.narrative.beats or the resolved template). A single string declares the slide's primary beat; an array declares that one slide covers multiple beats — useful when shorter decks fold beats together (e.g. ['problem', 'why-now']). Declares the slide's role in the story arc; does not constrain ordering or structure. Slides without a beat are valid; multiple slides may share a beat."
        }
      }
    },
    "Transition": {
      "type": "object",
      "description": "Animated transition used when entering a slide.",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["none", "fade", "slide", "push", "wipe", "morph", "zoom"],
          "description": "Transition style."
        },
        "duration": {
          "type": "number",
          "description": "Transition duration in seconds."
        },
        "direction": {
          "type": "string",
          "enum": ["left", "right", "up", "down"],
          "description": "Direction for directional transitions (e.g., slide, push, wipe)."
        }
      }
    },
    "Position": {
      "type": "object",
      "description": "Element position on the slide.",
      "properties": {
        "x": {
          "type": "number",
          "description": "Horizontal coordinate; interpretation depends on the containing element's units."
        },
        "y": {
          "type": "number",
          "description": "Vertical coordinate; interpretation depends on the containing element's units."
        },
        "anchor": {
          "type": "string",
          "description": "Anchor to layout grid, e.g. 'top-left', 'center', or 'bottom-right'."
        }
      }
    },
    "Size": {
      "type": "object",
      "description": "Element size on the slide.",
      "properties": {
        "width": {
          "type": "number",
          "description": "Element width expressed in the unit specified by 'unit'."
        },
        "height": {
          "type": "number",
          "description": "Element height expressed in the unit specified by 'unit'."
        },
        "unit": {
          "type": "string",
          "enum": ["in", "%", "px"],
          "description": "Unit for width and height: 'in' (inches), '%' (percentage of slide), or 'px' (pixels)."
        }
      }
    },
    "Element": {
      "type": "object",
      "description": "A renderable item on a slide. The 'type' discriminator selects which additional fields are valid.",
      "required": ["id", "type"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the element within its slide."
        },
        "type": {
          "type": "string",
          "enum": ["text", "image", "shape", "chart", "table", "video", "code", "group", "placeholder"],
          "description": "Element kind, which determines the additional fields available."
        },
        "position": {
          "$ref": "#/$defs/Position",
          "description": "Explicit element position; when omitted, the element is positioned by the layout."
        },
        "size": {
          "$ref": "#/$defs/Size",
          "description": "Explicit element size; when omitted, the element is sized by the layout."
        },
        "slot": {
          "type": "string",
          "description": "Named slot from the layout template, e.g. 'title', 'body', or 'image-right'."
        },
        "animation": {
          "$ref": "#/$defs/Animation",
          "description": "Animation applied to this element."
        }
      },
      "allOf": [
        {
          "if": { "properties": { "type": { "const": "text" } } },
          "then": {
            "properties": {
              "content": {
                "$ref": "#/$defs/TextContent",
                "description": "Text content as plain text, formatted runs, markdown, or list items."
              },
              "style": {
                "$ref": "#/$defs/TextStyle",
                "description": "Text styling applied to the entire content."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "image" } } },
          "then": {
            "required": ["src"],
            "properties": {
              "src": {
                "type": "string",
                "description": "Source URL or asset reference for the image."
              },
              "alt": {
                "type": "string",
                "description": "Accessible alt text describing the image."
              },
              "fit": {
                "type": "string",
                "enum": ["cover", "contain", "fill", "none"],
                "description": "How the image fills its bounding box."
              },
              "borderRadius": {
                "type": "number",
                "description": "Corner radius in points; produces rounded corners when greater than 0."
              },
              "shadow-sm": {
                "type": "boolean",
                "description": "Whether to render a small drop shadow behind the image."
              },
              "caption": {
                "type": "string",
                "description": "Caption rendered beneath the image."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "chart" } } },
          "then": {
            "required": ["chartType", "data"],
            "properties": {
              "chartType": {
                "type": "string",
                "enum": ["bar", "column", "line", "pie", "donut", "area", "scatter", "radar", "waterfall", "funnel", "treemap", "combo"],
                "description": "Chart variant to render."
              },
              "data": {
                "$ref": "#/$defs/ChartData",
                "description": "Chart data, including labels and datasets."
              },
              "options": {
                "$ref": "#/$defs/ChartOptions",
                "description": "Visual and behavioral chart options."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "table" } } },
          "then": {
            "required": ["rows"],
            "properties": {
              "headers": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Header row labels rendered above the data rows."
              },
              "rows": {
                "type": "array",
                "items": { "type": "array" },
                "description": "Two-dimensional array of row data; each row aligns by index with the headers/columns."
              },
              "style": {
                "$ref": "#/$defs/TableStyle",
                "description": "Visual styling applied to the table."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "shape" } } },
          "then": {
            "required": ["shape"],
            "properties": {
              "shape": {
                "type": "string",
                "description": "Shape type, e.g. 'rectangle', 'circle', 'ellipse', 'triangle', 'arrow', 'line', 'star', 'callout', or 'custom'."
              },
              "fill": {
                "type": "string",
                "description": "Fill color as a hex string."
              },
              "stroke": {
                "type": "object",
                "description": "Outline stroke for the shape.",
                "properties": {
                  "color": {
                    "type": "string",
                    "description": "Stroke color as a hex string."
                  },
                  "width": {
                    "type": "number",
                    "description": "Stroke width in points."
                  }
                }
              },
              "cornerRadius": {
                "type": "number",
                "description": "Corner radius in points (used for rectangles)."
              },
              "text": {
                "$ref": "#/$defs/TextContent",
                "description": "Text rendered inside the shape."
              },
              "path": {
                "type": "string",
                "description": "SVG path data used when shape is 'custom'."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "code" } } },
          "then": {
            "required": ["code"],
            "properties": {
              "code": {
                "type": "string",
                "description": "Source code to display."
              },
              "language": {
                "type": "string",
                "description": "Language identifier used for syntax highlighting (e.g., 'python', 'ts')."
              },
              "theme": {
                "type": "string",
                "enum": ["dark", "light"],
                "description": "Syntax highlighting theme."
              },
              "showLineNumbers": {
                "type": "boolean",
                "description": "Whether to render line numbers alongside the code."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "group" } } },
          "then": {
            "required": ["children"],
            "properties": {
              "children": {
                "type": "array",
                "items": { "$ref": "#/$defs/Element" },
                "description": "Child elements rendered as a group; their positions and sizes nest within the group's bounds."
              }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "placeholder" } } },
          "then": {
            "required": ["prompt"],
            "properties": {
              "prompt": {
                "type": "string",
                "description": "AI instruction describing what content should be generated for this slot."
              },
              "expectedType": {
                "type": "string",
                "enum": ["text", "image", "chart", "table"],
                "description": "Expected content type the engine or agent should produce."
              }
            }
          }
        }
      ]
    },
    "Animation": {
      "type": "object",
      "description": "Animation effect applied to an element.",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["appear", "fadeIn", "slideIn", "zoomIn", "typewriter", "custom"],
          "description": "Animation effect applied to the element."
        },
        "delay": {
          "type": "number",
          "description": "Delay before the animation starts, in seconds."
        },
        "duration": {
          "type": "number",
          "description": "Animation duration in seconds."
        },
        "order": {
          "type": "integer",
          "description": "Order in the slide's animation sequence; lower values play first."
        }
      }
    },
    "TextContent": {
      "type": "object",
      "description": "Text content expressed as plain text, rich runs, markdown, or list items.",
      "properties": {
        "text": {
          "type": "string",
          "description": "Simple text string for basic use; ignored when 'runs' or 'markdown' is provided."
        },
        "runs": {
          "type": "array",
          "items": { "$ref": "#/$defs/TextRun" },
          "description": "Rich text expressed as formatted runs."
        },
        "markdown": {
          "type": "string",
          "description": "Markdown source converted to runs by the engine."
        },
        "bullets": {
          "type": "array",
          "description": "Bulleted list items; each item is either a string or a nested TextContent."
        },
        "numbered": {
          "type": "array",
          "description": "Numbered list items; each item is either a string or a nested TextContent."
        }
      }
    },
    "TextRun": {
      "type": "object",
      "description": "A contiguous run of text sharing the same character formatting.",
      "required": ["text"],
      "properties": {
        "text": {
          "type": "string",
          "description": "Text for this run."
        },
        "bold": {
          "type": "boolean",
          "description": "Whether the run is rendered in bold."
        },
        "italic": {
          "type": "boolean",
          "description": "Whether the run is rendered in italic."
        },
        "underline": {
          "type": "boolean",
          "description": "Whether the run is underlined."
        },
        "strikethrough": {
          "type": "boolean",
          "description": "Whether the run is rendered with a strikethrough line."
        },
        "color": {
          "type": "string",
          "description": "Run text color as a hex string."
        },
        "fontSize": {
          "type": "number",
          "description": "Run font size in points."
        },
        "fontFamily": {
          "type": "string",
          "description": "Run font family override."
        },
        "link": {
          "type": "string",
          "description": "URL to link the run text to."
        },
        "superscript": {
          "type": "boolean",
          "description": "Whether the run is rendered as superscript."
        },
        "subscript": {
          "type": "boolean",
          "description": "Whether the run is rendered as subscript."
        }
      }
    },
    "TextStyle": {
      "type": "object",
      "description": "Paragraph-level text styling applied to a TextContent.",
      "properties": {
        "fontSize": {
          "type": "number",
          "description": "Font size in points."
        },
        "fontFamily": {
          "type": "string",
          "description": "Font family override."
        },
        "color": {
          "type": "string",
          "description": "Text color as a hex string."
        },
        "alignment": {
          "type": "string",
          "enum": ["left", "center", "right", "justify"],
          "description": "Horizontal text alignment."
        },
        "lineHeight": {
          "type": "number",
          "description": "Line height multiplier (e.g., 1.5)."
        },
        "letterSpacing": {
          "type": "number",
          "description": "Letter spacing (tracking) in ems."
        },
        "textTransform": {
          "type": "string",
          "enum": ["none", "uppercase", "lowercase", "capitalize"],
          "description": "Capitalization transform applied to the text."
        },
        "verticalAlignment": {
          "type": "string",
          "enum": ["top", "middle", "bottom"],
          "description": "Vertical alignment within the text frame."
        }
      }
    },
    "ChartData": {
      "type": "object",
      "description": "Data driving a chart element.",
      "required": ["datasets"],
      "properties": {
        "labels": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Category labels shared across datasets (e.g., x-axis tick labels)."
        },
        "datasets": {
          "type": "array",
          "description": "One or more data series rendered on the chart.",
          "items": {
            "type": "object",
            "description": "A single data series.",
            "required": ["label", "values"],
            "properties": {
              "label": {
                "type": "string",
                "description": "Dataset name shown in the legend."
              },
              "values": {
                "type": "array",
                "items": { "type": "number" },
                "description": "Numeric values for the dataset; aligns by index with the chart's labels."
              },
              "color": {
                "type": "string",
                "description": "Override color for this dataset as a hex string."
              },
              "type": {
                "type": "string",
                "description": "Per-dataset chart type override (used by combo charts, e.g., 'line' or 'column')."
              }
            }
          }
        }
      }
    },
    "ChartOptions": {
      "type": "object",
      "description": "Visual and behavioral options for a chart element.",
      "properties": {
        "title": {
          "type": "string",
          "description": "Chart title shown above the plot."
        },
        "legend": {
          "description": "Legend configuration: pass a boolean to toggle the legend, or an object such as { position: 'top' | 'bottom' | 'left' | 'right' } to customize placement."
        },
        "showValues": {
          "type": "boolean",
          "description": "Whether to render data labels next to each value."
        },
        "showGrid": {
          "type": "boolean",
          "description": "Whether to render gridlines on the chart."
        },
        "stacked": {
          "type": "boolean",
          "description": "Whether series should be stacked rather than grouped."
        },
        "yAxis": {
          "type": "object",
          "description": "Y-axis configuration.",
          "properties": {
            "label": {
              "type": "string",
              "description": "Y-axis title."
            },
            "min": {
              "type": "number",
              "description": "Minimum y-axis value."
            },
            "max": {
              "type": "number",
              "description": "Maximum y-axis value."
            },
            "format": {
              "type": "string",
              "description": "Tick label format string (e.g., '$#,##0' or '0.0%')."
            }
          }
        },
        "xAxis": {
          "type": "object",
          "description": "X-axis configuration.",
          "properties": {
            "label": {
              "type": "string",
              "description": "X-axis title."
            },
            "format": {
              "type": "string",
              "description": "Tick label format string."
            }
          }
        }
      }
    },
    "TableStyle": {
      "type": "object",
      "description": "Visual styling applied to a table element.",
      "properties": {
        "headerBackground": {
          "type": "string",
          "description": "Header row background color as a hex string."
        },
        "headerColor": {
          "type": "string",
          "description": "Header row text color as a hex string."
        },
        "stripedRows": {
          "type": "boolean",
          "description": "Whether to alternate row backgrounds for readability."
        },
        "borderColor": {
          "type": "string",
          "description": "Color of cell borders as a hex string."
        },
        "compact": {
          "type": "boolean",
          "description": "Use a denser row height and reduced padding."
        }
      }
    }
  }
}
